
####### DOCKER IMAGES
.debian-image:
  image: blockstream/gdk-debian-ci@sha256:2e1204317f43861a674c3dc9b9a6b9f970e190e95d4c5f3a869f000a5bd926c2
  tags: [ ga ]

.android-image:
  image: blockstream/gdk-android-builder@sha256:854726212aeed097667337f3f78ba2be59289c5c5ece8fbba0e7f59b17f45ea6
  tags: [ ga ]

.python-image:
  image: blockstream/gdk-python-builder@sha256:df02a7190699306733e863da2c15edfbe290859219cd5b95cc66bb4346b3a707
  tags: [ ga ]

.ubuntu-image:
  image: blockstream/gdk-ubuntu-builder@sha256:97a1cca3bc13455759bcf801542a924f29b69838d51c875eea3c3dc236213025
  tags: [ ga ]

.ubuntu-arm64-image:
  image: blockstream/gdk-ubuntu-builder@sha256:448125e02ae4ce1a365a6704c5597f5e7137416e0cec9638154621ba32d656f9
  tags: [ arm64_docker ]

.fedora-image:
  image: blockstream/gdk-fedora-builder@sha256:2778cc8b0a31e6820eeb894042ec3c426d0d21b3f2131b878d2acba72af2bbbe

.aws-lambda-image:
  image: blockstream/gdk-aws-lambda-builder@sha256:073963511949ca31115faf2f40d024509db7617314f663bd2e1e62689698cc2d
  tags: [ ga ]


.py_release:
  variables:
    INSTALL_PATH: "gdk-python"
  artifacts:
    name: "$CI_JOB_NAME-$CI_COMMIT_SHORT_SHA"
    expire_in: 1 day
    when: on_success
    paths:
    - wheelhouse


.gcloud-publish:
  image: blockstream/gcloud-docker-tf:1.1.7
  tags:
    - ga
  stage: publish
  variables:
    GCLOUD_URL: "gs://green-gdk-builds"
  before_script:
    - subfolder=$CI_PROJECT_NAME-$CI_COMMIT_SHA
    - TMPF=$(mktemp) || exit 1
    - echo $GCLOUD_PUSH_KEY > $TMPF
    - export GOOGLE_APPLICATION_CREDENTIALS=$TMPF
    - gcloud auth activate-service-account --key-file=$TMPF
    - rm -f $TMPF


##### WARNING!!!!
    # $PREBUILT_SUBDIR is the folder for the DIY caching system we have in place in mac machines
    # BUILD_IDX is your life belt in case you messed the CI up during the prebuild phase and the PREBUILT_DIR folder
    # is left in an unconsistent state, just change the BUILD_IDX and you are good to go.
.osx_env:
  variables:
    BUILD_IDX: "0"
    GDK_KEEP_DOWNLOADS: "1"
    UV_CACHE_DIR: "$CI_BUILDS_DIR/downloads/uv-cache"
    UV_PYTHON_INSTALL_DIR: "$CI_BUILDS_DIR/downloads/uv-python"
  before_script:
    - idx=($(shasum tools/* cmake/profiles/* | shasum))
    - export PREBUILT_SUBDIR="prebuilt-${idx}-${BUILD_IDX}"
    - echo "prebuild subdir is ${PREBUILT_SUBDIR}"
    - mkdir -p $CI_BUILDS_DIR/downloads # Global shared download directory
    - mkdir -p "$UV_CACHE_DIR" "$UV_PYTHON_INSTALL_DIR"
    - ln -s $CI_BUILDS_DIR/downloads downloads
    - brew install llvm@17
    - export BREW_PREFIX=$( [[ $(uname -m) == "arm64" ]] && echo "/opt/homebrew" || echo "/usr/local" )
    - export LLVM_PATH="$BREW_PREFIX/opt/llvm@17"
    - export PATH="$LLVM_PATH/bin:$PATH"
    - export CC="$LLVM_PATH/bin/clang"
    - export CXX="$LLVM_PATH/bin/clang++"
    - export LDFLAGS="-L$LLVM_PATH/lib"
    - export CPPFLAGS="-I$LLVM_PATH/include"
    - export CMAKE_PREFIX_PATH="$LLVM_PATH"
