
####### DOCKER IMAGES
.debian-image:
  image: blockstream/gdk-debian-ci@sha256:8c6e5eb5f164e45f09ef3f5374730e2b5488dbfc125f8a1fe96f15d6cbec7e30
  tags: [ ga ]

.android-image:
  image: blockstream/gdk-android-builder@sha256:7382e771c64e8c81af5a50e08c6dfb8d19b0dbb8f2e1f7207930a67b3015a78e
  tags: [ ga ]

.python-image:
  image: blockstream/gdk-python-builder@sha256:69d9f4e4ad1a1489da2a9125f76133d31fdbe331e18e11f0f7de69aae55f585e
  tags: [ ga ]

.ubuntu-image:
  image: blockstream/gdk-ubuntu-builder@sha256:d6ac9f945c61288cf3dcc5912999d9f5d8d74eed107aaef72e5739dcc86c5ecf
  tags: [ ga ]

.ubuntu-arm64-image:
  image: blockstream/gdk-ubuntu-builder@sha256:1c522d8ec67a22e2cc95bf7a4cd33b25efad7ce43804fba8aef3255f7da041c6
  tags: [ arm64_docker ]

.fedora-image:
  image: blockstream/gdk-fedora-builder@sha256:fac230dbf919058d2287c46d62136e8154f6a0b7b2e80eb3ccbac9faa06500f5

.aws-lambda-image:
  image: blockstream/gdk-aws-lambda-builder@sha256:8da118751dc0538e232ad23b0b2e1aff7ad4b010139ba1e1227c2a87ed28769c
  tags: [ ga ]


.py_release:
  variables:
    INSTALL_PATH: "gdk-python"
  artifacts:
    name: "$CI_JOB_NAME-$CI_COMMIT_SHORT_SHA"
    expire_in: 1 day
    when: on_success
    paths:
    - wheelhouse


.gcloud-publish:
  image: blockstream/gcloud-docker-tf:1.1.7
  tags:
    - ga
  stage: publish
  variables:
    GCLOUD_URL: "gs://green-gdk-builds"
  before_script:
    - subfolder=$CI_PROJECT_NAME-$CI_COMMIT_SHA
    - TMPF=$(mktemp) || exit 1
    - echo $GCLOUD_PUSH_KEY > $TMPF
    - export GOOGLE_APPLICATION_CREDENTIALS=$TMPF
    - gcloud auth activate-service-account --key-file=$TMPF
    - rm -f $TMPF


##### WARNING!!!!
    # $PREBUILT_SUBDIR is the folder for the DIY caching system we have in place in mac machines
    # BUILD_IDX is your life belt in case you messed the CI up during the prebuild phase and the PREBUILT_DIR folder
    # is left in an unconsistent state, just change the BUILD_IDX and you are good to go.
.osx_env:
  variables:
    BUILD_IDX: "0"
    GDK_KEEP_DOWNLOADS: "1"
    PYTHON_VERSION: "3.9"
  before_script:
    - idx=($(shasum tools/* cmake/profiles/* | shasum))
    - export PREBUILT_SUBDIR="prebuilt-${idx}-${BUILD_IDX}"
    - echo "prebuild subdir is ${PREBUILT_SUBDIR}"
    - mkdir -p $CI_BUILDS_DIR/downloads # Global shared download directory
    - ln -s $CI_BUILDS_DIR/downloads downloads
    - brew install llvm@17
    - export BREW_PREFIX=$( [[ $(uname -m) == "arm64" ]] && echo "/opt/homebrew" || echo "/usr/local" )
    - export LLVM_PATH="$BREW_PREFIX/opt/llvm@17"
    - export PATH="$LLVM_PATH/bin:$PATH"
    - export CC="$LLVM_PATH/bin/clang"
    - export CXX="$LLVM_PATH/bin/clang++"
    - export LDFLAGS="-L$LLVM_PATH/lib"
    - export CPPFLAGS="-I$LLVM_PATH/include"
    - export CMAKE_PREFIX_PATH="$LLVM_PATH"
