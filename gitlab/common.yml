
####### DOCKER IMAGES
.debian-image:
  image: blockstream/gdk-debian-ci@sha256:e85ff89aeb745e320ed80b101b06bce41cf32f13fdaecd4e9fa1a672023e1035
  tags: [ ga ]

.android-image:
  image: blockstream/gdk-android-builder@sha256:5d02e06d32f580992083f73dccdf143137702a4f835f23117dfe71e0fac1ec2c
  tags: [ ga ]

.python-image:
  image: blockstream/gdk-python-builder@sha256:3ce83469b8ede594a25f99a7b89225960a40164168d1603559d47b339c7f7098
  tags: [ ga ]

.ubuntu-image:
  image: blockstream/gdk-ubuntu-builder@sha256:f3e4384359ec75eef1782ac8be943577a3a91b9116f9c6a9086a46c6b36de1c0
  tags: [ ga ]

.ubuntu-arm64-image:
  image: blockstream/gdk-ubuntu-builder@sha256:380707f1beab151bfa0af8707c37fa57a3d4327350a67c22e342143f0c0e77b2
  tags: [ arm64_docker ]

.fedora-image:
  image: blockstream/gdk-fedora-builder@sha256:e10f268aa58d12495ea8cb0d6c94daf4a7c18588b6e226ca4b8d63b8ffb75dfd
  tags: [ ga ]

.aws-lambda-image:
  image: blockstream/gdk-aws-lambda-builder@sha256:0171050d957d749eb6fc0bc6ff0bdd388be91eb60a692f4ec609daf9479c5e69
  tags: [ ga ]


.py_release:
  variables:
    INSTALL_PATH: "gdk-python"
  artifacts:
    name: "$CI_JOB_NAME-$CI_COMMIT_SHORT_SHA"
    expire_in: 1 day
    when: on_success
    paths:
    - wheelhouse


.gcloud-publish:
  image: blockstream/gcloud-docker-tf:1.1.7
  tags:
    - ga
  stage: publish
  variables:
    GCLOUD_URL: "gs://green-gdk-builds"
  before_script:
    - subfolder=$CI_PROJECT_NAME-$CI_COMMIT_SHA
    - TMPF=$(mktemp) || exit 1
    - echo $GCLOUD_PUSH_KEY > $TMPF
    - export GOOGLE_APPLICATION_CREDENTIALS=$TMPF
    - gcloud auth activate-service-account --key-file=$TMPF
    - rm -f $TMPF


##### WARNING!!!!
    # $PREBUILT_SUBDIR is the folder for the DIY caching system we have in place in mac machines
    # BUILD_IDX is your life belt in case you messed the CI up during the prebuild phase and the PREBUILT_DIR folder
    # is left in an unconsistent state, just change the BUILD_IDX and you are good to go.
.osx_env:
  variables:
    BUILD_IDX: "0"
    GDK_KEEP_DOWNLOADS: "1"
    PYTHON_VERSION: "3.9"
  before_script:
    - idx=($(shasum tools/* cmake/profiles/* | shasum))
    - export PREBUILT_SUBDIR="prebuilt-${idx}-${BUILD_IDX}"
    - echo "prebuild subdir is ${PREBUILT_SUBDIR}"
    - mkdir -p $CI_BUILDS_DIR/downloads # Global shared download directory
    - ln -s $CI_BUILDS_DIR/downloads downloads
    - brew install llvm@17
    - export BREW_PREFIX=$( [[ $(uname -m) == "arm64" ]] && echo "/opt/homebrew" || echo "/usr/local" )
    - export LLVM_PATH="$BREW_PREFIX/opt/llvm@17"
    - export PATH="$LLVM_PATH/bin:$PATH"
    - export CC="$LLVM_PATH/bin/clang"
    - export CXX="$LLVM_PATH/bin/clang++"
    - export LDFLAGS="-L$LLVM_PATH/lib"
    - export CPPFLAGS="-I$LLVM_PATH/include"
    - export CMAKE_PREFIX_PATH="$LLVM_PATH"
