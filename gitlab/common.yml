
####### DOCKER IMAGES
.debian-image:
  image: blockstream/gdk-debian-ci@sha256:96447f967a7332aace7105284d3912d6728668bafeef2c8f21662b6498e6c5a7
  tags: [ ga ]

.android-image:
  image: blockstream/gdk-android-builder@sha256:db08bea0ab3298813ffeb08a6ca8a3473bb2bcfba0fd74fc61793ae62eb21cbf
  tags: [ ga ]

.python-image:
  image: blockstream/gdk-python-builder@sha256:877852df7968ee7c1fedc05f1a56ca171c9afbfcd776c678b8c3184e35323201
  tags: [ ga ]

.ubuntu-image:
  image: blockstream/gdk-ubuntu-builder@sha256:9a739abc45885c8f865e2181f4037160f489ce9e6e877d883799bfa7ced97a76
  tags: [ ga ]

.fedora-image:
  image: blockstream/gdk-fedora-builder@sha256:a1b05d8ff8f3be2048c7d95b27086cb8c5d37a88ad27f0f387818150da28bb5a
  tags: [ ga ]


.py_release:
  variables:
    INSTALL_PATH: "gdk-python"
  artifacts:
    name: "$CI_JOB_NAME-$CI_COMMIT_SHORT_SHA"
    expire_in: 1 day
    when: on_success
    paths:
    - wheelhouse


.gcloud-publish:
  image: blockstream/gcloud-docker-tf:1.1.7
  tags:
    - ga
  stage: publish
  variables:
    GCLOUD_URL: "gs://green-gdk-builds"
  before_script:
    - subfolder=$CI_PROJECT_NAME-$CI_COMMIT_SHA
    - if [[ ${CI_COMMIT_REF_NAME} == "master" ]]; then subfolder=gdk-master; fi
    - TMPF=$(mktemp) || exit 1
    - echo $GCLOUD_PUSH_KEY > $TMPF
    - export GOOGLE_APPLICATION_CREDENTIALS=$TMPF
    - gcloud auth activate-service-account --key-file=$TMPF
    - rm -f $TMPF



##### WARNING!!!!
    # $PREBUILT_SUBDIR is the folder for the DIY caching system we have in place in mac machines
    # BUILD_IDX is your life belt in case you messed the CI up during the prebuild phase and the PREBUILT_DIR folder
    # is left in an unconsistent state, just change the BUILD_IDX and you are good to go.
    # when merged, go through all mac machines to delete old version of cache and reclaim some storage space
.osx_env:
  variables:
    BUILD_IDX: "1"
  before_script:
    - idx=($(shasum tools/* cmake/profiles/* | shasum))
    - export PREBUILT_SUBDIR="prebuilt-${idx}-${BUILD_IDX}"
