
####### DOCKER IMAGES
.debian-image:
  image: blockstream/gdk-debian-ci@sha256:4d56c9c2ab380b3657cea3c4e8ce458a583f637e839dafcff8c7311c5c9d5366
  tags: [ ga ]

.android-image:
  image: blockstream/gdk-android-builder@sha256:4c8103fff0499fa4807ed09ecb1a862c360d60911fa739b777a8c8baed41b9a2
  tags: [ ga ]

.python-image:
  image: blockstream/gdk-python-builder@sha256:fc91e8ad4370e74d7f7fef4286e9d4349e7f6520cf6c8b2403509f9122a54d0f
  tags: [ ga ]

.ubuntu-image:
  image: blockstream/gdk-ubuntu-builder@sha256:79b086bd458eeb0c294a84440818c1db3277d923bbb96429be31fa0a360ab2b8
  tags: [ ga ]

.ubuntu-arm64-image:
  image: blockstream/gdk-ubuntu-builder@sha256:d8c3359e02b483d39412cb1ff97aa74e5869e162f1bb41b8d37a24ad5d378481
  tags: [ arm64_docker ]

.fedora-image:
  image: blockstream/gdk-fedora-builder@sha256:ea08999e1b2c01f5775d1f85bed5db3ec76eb3b190e3fbf109a24872be0b1c43
  tags: [ ga ]

.aws-lambda-image:
  image: blockstream/gdk-aws-lambda-builder@sha256:067de138533dbf80d0a651ac17b881c60beb747bc038310819b961996ee0b219
  tags: [ ga ]


.py_release:
  variables:
    INSTALL_PATH: "gdk-python"
  artifacts:
    name: "$CI_JOB_NAME-$CI_COMMIT_SHORT_SHA"
    expire_in: 1 day
    when: on_success
    paths:
    - wheelhouse


.gcloud-publish:
  image: blockstream/gcloud-docker-tf:1.1.7
  tags:
    - ga
  stage: publish
  variables:
    GCLOUD_URL: "gs://green-gdk-builds"
  before_script:
    - subfolder=$CI_PROJECT_NAME-$CI_COMMIT_SHA
    - TMPF=$(mktemp) || exit 1
    - echo $GCLOUD_PUSH_KEY > $TMPF
    - export GOOGLE_APPLICATION_CREDENTIALS=$TMPF
    - gcloud auth activate-service-account --key-file=$TMPF
    - rm -f $TMPF



##### WARNING!!!!
    # $PREBUILT_SUBDIR is the folder for the DIY caching system we have in place in mac machines
    # BUILD_IDX is your life belt in case you messed the CI up during the prebuild phase and the PREBUILT_DIR folder
    # is left in an unconsistent state, just change the BUILD_IDX and you are good to go.
.osx_env:
  variables:
    BUILD_IDX: "0"
    GDK_KEEP_DOWNLOADS: "1"
    PYTHON_VERSION: "3.9"
  before_script:
    - idx=($(shasum tools/* cmake/profiles/* | shasum))
    - export PREBUILT_SUBDIR="prebuilt-${idx}-${BUILD_IDX}"
    - echo "prebuild subdir is ${PREBUILT_SUBDIR}"
    - mkdir -p $CI_BUILDS_DIR/downloads # Global shared download directory
    - ln -s $CI_BUILDS_DIR/downloads downloads
