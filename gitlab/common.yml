
####### DOCKER IMAGES
.debian-image:
  image: blockstream/gdk-debian-ci@sha256:baacda1c8e0137dfba9b0cd93cbcaccaf9bebec9b53eec5fa05e3d4b08540cb1
  tags: [ ga ]

.android-image:
  image: blockstream/gdk-android-builder@sha256:80385a86338ec462e024cd4f81ebe354b188e0299e13b98d292a21be81ebcc8c
  tags: [ ga ]

.python-image:
  image: blockstream/gdk-python-builder@sha256:e987e85146e29eb523b3dd95f352da0f4ec2065280bcdc042d84fea0e2ef83e7
  tags: [ ga ]

.ubuntu-image:
  image: blockstream/gdk-ubuntu-builder@sha256:976668ea49d738c79833c76149211d189416855913fbc5281a9a022e12330b5a
  tags: [ ga ]

.fedora-image:
  image: blockstream/gdk-fedora-builder@sha256:944ad4983fa23a8500c753dc95aa33b3316bc534801564144eab447b43ffcbe1
  tags: [ ga ]


.py_release:
  variables:
    INSTALL_PATH: "gdk-python"
  artifacts:
    name: "$CI_JOB_NAME-$CI_COMMIT_SHORT_SHA"
    expire_in: 1 day
    when: on_success
    paths:
    - wheelhouse


.gcloud-publish:
  image: blockstream/gcloud-docker-tf:1.1.7
  tags:
    - ga
  stage: publish
  variables:
    GCLOUD_URL: "gs://green-gdk-builds"
  before_script:
    - subfolder=$CI_PROJECT_NAME-$CI_COMMIT_SHA
    - if [[ ${CI_COMMIT_REF_NAME} == "master" ]]; then subfolder=gdk-master; fi
    - TMPF=$(mktemp) || exit 1
    - echo $GCLOUD_PUSH_KEY > $TMPF
    - export GOOGLE_APPLICATION_CREDENTIALS=$TMPF
    - gcloud auth activate-service-account --key-file=$TMPF
    - rm -f $TMPF



##### WARNING!!!!
    # $PREBUILT_SUBDIR is the folder for the DIY caching system we have in place in mac machines
    # BUILD_IDX is your life belt in case you messed the CI up during the prebuild phase and the PREBUILT_DIR folder
    # is left in an unconsistent state, just change the BUILD_IDX and you are good to go.
.osx_env:
  variables:
    BUILD_IDX: "2"
    GDK_KEEP_DOWNLOADS: "1"
  before_script:
    - idx=($(shasum tools/* cmake/profiles/* | shasum))
    - export PREBUILT_SUBDIR="prebuilt-${idx}-${BUILD_IDX}"
    - mkdir -p $CI_BUILDS_DIR/downloads # Global shared download directory
    - ln -s $CI_BUILDS_DIR/downloads downloads
